@model _5051.Models.StudentModel

@{
    ViewBag.Title = "Update";
}


<div class="container">
    <div class="row">

        @{
            if (Model == null)
            {
                <p>No Student</p>
            }

            else
            {
                <div class="row">
                    <h2>Read Student</h2>
                    <br />
                    <br />

                    <div class="row">
                        <div class="col-sm-2 text-right">
                        </div>
                        <div class="col-sm-2">
                            <img src="~/Content/img/@Model.AvatarId" width="75" height="75" />
                            <br />
                            <span>
                                @Model.Name
                                <br />
                                <br />
                                <br />
                            </span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-2 text-right">
                            @Html.DisplayNameFor(m => m.Name)
                        </div>
                        <div class="col-sm-2">
                            @Html.DisplayFor(m => m.Name)
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-2 text-right">
                            @Html.DisplayNameFor(m => m.AvatarId)
                        </div>
                        <div class="col-sm-2">
                            @Html.DisplayFor(m => m.AvatarId)
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-2 text-right">
                            @Html.DisplayNameFor(m => m.AvatarLevel)
                        </div>
                        <div class="col-sm-2">
                            @Html.DisplayFor(m => m.AvatarLevel)
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-2 text-right">
                            @Html.DisplayNameFor(m => m.Tokens)
                        </div>
                        <div class="col-sm-2">
                            @Html.DisplayFor(m => m.Tokens)
                        </div>
                    </div>


                    <br />
                    <br />
                    <div class="row">
                        <div class="col-sm-2 text-right">
                        </div>
                        <div class="col-sm-4">
                            @Html.ActionLink("Cancel", "Index", new { id = Model.Id }, new { @class = "btn btn-success" })
                            @Html.ActionLink("Update", "Update", new { id = Model.Id }, new { @class = "btn btn-primary" })
                            @Html.ActionLink("Delete", "Delete", new { id = Model.Id }, new { @class = "btn btn-danger" })
                        </div>
                    </div>



                </div>
            }

        }
    </div>
</div>
