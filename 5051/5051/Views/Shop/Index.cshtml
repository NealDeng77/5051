@model _5051.Models.StudentModel
@{
    ViewBag.Title = "Shop";
}
<br />
<h2>Welcome to the Store</h2>

<h3>Spend your Tokens on items at the store</h3>

<br />

<div class="ImageBox">

    <div class="TopperBox">
        <img class="TopperItem" id="Topper" width="1000" height="800" src="" alt="Topper">
    </div>

    <div class="SignBox">
        <img class="SignItem" id="Sign" width="1000" height="800" src="" alt="Sign">
    </div>

    <div class="TruckBox">
        <img class="TruckItem" id="Truck" width="1000" height="800" src="" alt="Truck">
    </div>

    <div class="MenuBox">
        <img class="MenuItem" id="Menu" width="1000" height="800" src="" alt="Menu">
    </div>

    <div class="WheelsBox">
        <img class="WheelsItem" id="Wheels" width="1000" height="800" src="" alt="Wheels">
    </div>

    <div class="TrailerBox">
        <img class="TrailerItem" id="Trailer" width="1000" height="800" src="" alt="Trailer">
    </div>

</div>

<br />
<br />

<style>

    .ImageBox {
        position: relative;
        width: 1000px;
        height: 1000px;
    }

    .TopperBox {
        position: absolute;
        padding-left: 500px;
        z-index: 1;
    }

    .TruckBox {
        position: absolute;
        padding-top: 100px;
        z-index: 2;
    }

    .MenuBox {
        position: absolute;
        padding-top: 190px;
        padding-left: 750px;
        z-index: 3;
    }

    .SignBox {
        position: absolute;
        padding-top: 600px;
        padding-left: 400px;
        z-index: 5;
    }

    .WheelsBox {
        position: absolute;
        padding-top: 490px;
        padding-left: 40px;
        z-index: 4;
    }

    .TrailerBox {
        position: absolute;
        padding-top: 330px;
        padding-left: 990px;
        z-index: 2;
    }


    .TopperItem {
        width: 100px;
        height: 100px;
    }

    .TruckItem {
        width: auto;
        height: auto;
    }

    .MenuItem {
        width: 135px;
        height: 175px;
    }

    .SignItem {
        width: 100px;
        height: 100px;
    }

    .WheelsItem {
        width: 1400px;
        height: 175px;
    }

    .TrailerItem {
        width: 500px;
        height: 250px;
    }
</style>


<br />
<br />




@Html.ActionLink("Discounts", "Discounts", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />
@Html.ActionLink("Special Items", "Specials", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />
@Html.ActionLink("Factory", "Factory", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />

@Html.ActionLink("Shop Inventory", "Inventory", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />

@Html.ActionLink("Celebrity Poster", "CelebrityPoster", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />

@Html.ActionLink("Edit Shop", "Edit", "Shop", new { id = Model.Id }, new { @class = "btn btn-primary" })

<br />
<br />

@{
    // Game Engine Code block

    // Put the Student ID into the Dom, so the Game engine can find it
    <div>
        @Html.Hidden("StudentId", Model.Id)
    </div>

    // Show the Current Iteration Number
    <div class="row">
        <span>Iteration Number:&nbsp;</span><span id="IterationNumber"></span>
    </div>

    // Show the Current Refresh Data
    <div class="row">
        <span>Game Data:&nbsp;</span><span id="GameData"></span>
    </div>

    <br />
    <br />
    <br />

    <script src="~/Scripts/Game.js"></script>
}